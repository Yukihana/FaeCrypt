<!--suppress XAMLIntellisenseDataContextBinding -->
<Window x:Class="Faenote.Wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:context="clr-namespace:Faenote.Logic.ViewContexts.MainViewContext;assembly=Faenote.Logic"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=context:MainContextLogic, IsDesignTimeCreatable=True}"
        Title="MainWindow" Height="450" Width="500"
        Closing="Window_Closing">
    <DockPanel>
        <ToolBarTray DockPanel.Dock="Top" IsLocked="True">
            <ToolBar>
                <Button Command="{Binding DefaultCommand}" CommandParameter="New">New</Button>
                <Button Click="Open_Click">Open</Button>
                <Separator />
                <Button>Save</Button>
                <Button>Save as</Button>
                <Separator />
                <ToggleButton>Quick-mode</ToggleButton>
            </ToolBar>
        </ToolBarTray>
        <TextBox x:Name="TextContent" BorderThickness="0" />
    </DockPanel>
</Window>